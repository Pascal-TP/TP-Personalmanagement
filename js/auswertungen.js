import { db } from "./firebase.js";
import { collection, getDocs } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";
import { setHead } from "./app.js";
import { esc } from "./utils.js";
export async function renderAuswertungen(el,ctx){setHead('Auswertungen','Erste übergreifende Übersicht für Personalabteilung und Vorgesetzte.');const [u,t,v]=await Promise.all([getDocs(collection(db,'users')),getDocs(collection(db,'trainings')),getDocs(collection(db,'vacationRequests'))]);let users=u.docs.map(d=>({id:d.id,...d.data()}));if(ctx.profile.role==='supervisor')users=users.filter(x=>x.id===ctx.profile.id||x.supervisorId===ctx.profile.id);const active=users.filter(x=>x.active!==false).length,pending=v.docs.map(d=>d.data()).filter(x=>x.status==='pending'&&(ctx.profile.role==='admin'||x.supervisorId===ctx.profile.id)).length;el.innerHTML=`<div class="kpi-grid three"><div class="kpi"><span>Aktive Mitarbeiter</span><strong>${active}</strong><small>im sichtbaren Bereich</small></div><div class="kpi"><span>Schulungen</span><strong>${t.size}</strong><small>im System angelegt</small></div><div class="kpi"><span>Offene Urlaubsfreigaben</span><strong>${pending}</strong><small>aktuell zu bearbeiten</small></div></div><article class="card"><div class="card-head"><div><h2>Auswertungen V1.0</h2><p>Dieser Bereich ist modular vorbereitet.</p></div></div><div class="info-strip">Später können hier u. a. Arbeitszeitkonten, Fehlzeiten, Urlaub, Schulungsquoten, Personalbewegungen und firmenbezogene Kennzahlen als eigene Auswertungsbausteine ergänzt werden.</div></article>`}
